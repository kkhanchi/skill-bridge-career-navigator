.env
__pycache__/
.pytest_cache/
*.pyc
.hypothesis/

# Phase 4 — Testing & Quality artifacts
.coverage
coverage.xml
.mypy_cache/
.ruff_cache/

# Dev databases (Phase 2 leftover — keep gitignored so local runs
# don't accidentally check in committed dev state).
*.db

# Phase 6 — Streamlit integration secrets
# Real values land in Streamlit Cloud's dashboard; local dev can
# override via .env or export API_BASE_URL inline.
.streamlit/secrets.toml
